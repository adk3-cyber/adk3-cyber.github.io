---
layout: post
title: RGB Loop
subtitle: Code which gives 3 sets of each RGB every 10 digits it goes up
gh-repo: daattali/beautiful-jekyll
gh-badge: [star, fork, follow]
tags: [test]
comments: true
mathjax: true
author: Arjun Kalra
---

**What this code does**

This program uses a loop to simulate turing on Red, Green, Blue leds by printing the colors instead of actually turning them on. It relies on a variable runtimes where every 10th iteration it prints a color. Each color is printed 3 times before it moves onto the next color. 

**Example of code where runtimes is set to 100**

![Example of code where runtimes is set to 100 which results in one set of red, green, and blue being pritned with an additional red at the end](https://adk3-cyber.github.io/assets/img/rgbloop.png)

**Tip for my past self**

Use numbers to make your life simpler. 
